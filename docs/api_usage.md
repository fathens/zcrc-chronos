# API使用ガイド

## 概要

zcrc-chronos APIは非同期処理ベースの時系列予測サービスを提供します。長時間実行される予測処理をバックグラウンドで実行し、進捗状況やキャンセルを管理できます。

## 利用可能なモデル

### 単一モデル（高速・特化型）

以下の単一モデルが利用可能です。それぞれ特定の用途に最適化されています：

| モデル名 | 説明 | 実行時間 | 推奨用途 |
|---------|------|----------|----------|
| `autoets` | AutoETS（統計的手法・高速） | 5分以内 | 短期予測、統計的手法 |
| `npts` | NPTS（Neural Prophet・高精度） | 10分以内 | 中期予測、機械学習 |
| `seasonal_naive` | SeasonalNaive（季節性ベースライン） | 2分以内 | ベースライン予測、超高速処理 |
| `recursive_tabular` | RecursiveTabular（勾配ブースティング） | 10分以内 | 中長期予測、高精度 |
| `chronos_zero_shot` | ChronosZeroShot（Transformer） | 15分以内 | 事前訓練済み、ゼロショット |
| `dynamic_theta` | DynamicOptimizedTheta（動的最適化） | 5分以内 | 中精度、バランス型 |
| `temporal_fusion_transformer` | TemporalFusionTransformer（注意機構） | 20分以内 | 最高精度、長期予測 |
| `deepar` | DeepAR（Amazon開発・深層学習） | 15分以内 | 深層学習、高精度 |

## API使用フロー

### 1. 非同期予測の開始

```bash
curl -X POST "http://localhost:8000/api/v1/predict_zero_shot_async" \
  -H "Content-Type: application/json" \
  -d '{
    "timestamp": [
      "2023-01-01T00:00:00",
      "2023-01-01T01:00:00",
      "2023-01-01T02:00:00"
    ],
    "values": [10.5, 11.2, 10.8],
    "forecast_until": "2023-01-04T02:00:00",
    "model_name": "autoets",
    "model_params": {}
  }'
```

**レスポンス:**
```json
{
  "task_id": "12345678-1234-5678-9012-123456789012",
  "status": "pending",
  "message": "予測タスクが正常に開始されました"
}
```

### 2. 予測ステータスの確認

```bash
curl -X GET "http://localhost:8000/api/v1/prediction_status/12345678-1234-5678-9012-123456789012"
```

**実行中のレスポンス:**
```json
{
  "task_id": "12345678-1234-5678-9012-123456789012",
  "status": "running",
  "progress": 0.6,
  "message": "モデル訓練中...",
  "created_at": "2023-01-01T00:00:00Z",
  "updated_at": "2023-01-01T00:01:30Z"
}
```

**完了時のレスポンス:**
```json
{
  "task_id": "12345678-1234-5678-9012-123456789012",
  "status": "completed",
  "progress": 1.0,
  "message": "予測処理が正常に完了しました",
  "result": {
    "forecast_timestamp": [
      "2023-01-01T03:00:00",
      "2023-01-01T04:00:00",
      "2023-01-01T05:00:00"
    ],
    "forecast_values": [10.9, 11.3, 11.7],
    "model_name": "autoets",
    "confidence_intervals": {
      "lower_95": [10.5, 10.8, 11.0],
      "upper_95": [11.3, 11.8, 12.4]
    },
    "metrics": {"mse": 0.15, "mae": 0.12}
  },
  "created_at": "2023-01-01T00:00:00Z",
  "updated_at": "2023-01-01T00:05:00Z"
}
```

### 3. 予測タスクのキャンセル

```bash
curl -X DELETE "http://localhost:8000/api/v1/prediction_cancel/12345678-1234-5678-9012-123456789012"
```

**レスポンス:**
```json
{
  "task_id": "12345678-1234-5678-9012-123456789012",
  "message": "タスクが正常にキャンセルされました",
  "cancelled": true
}
```

### 4. 全タスクの一覧取得

```bash
curl -X GET "http://localhost:8000/api/v1/prediction_tasks"
```

## 予測ステータス

| ステータス | 説明 |
|-----------|------|
| `pending` | タスクが開始されました（待機中） |
| `running` | 予測処理を実行中 |
| `completed` | 予測処理が正常に完了 |
| `failed` | 予測処理でエラーが発生 |
| `cancelled` | タスクがキャンセルされました |

## リクエストパラメータ

### ZeroShotPredictionRequest

| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `timestamp` | `List[datetime]` | ✓ | 時系列データのタイムスタンプ |
| `values` | `List[float]` | ✓ | 時系列データの値 |
| `forecast_until` | `datetime` | ✓ | 予測終了時点 |
| `model_name` | `string` | - | 使用するモデル名（デフォルト: "chronos_default"）。"chronos_default"はデフォルト設定を使用し、実際のモデルは内部で選択されます。 |
| `model_params` | `Dict[str, Any]` | - | モデル固有のパラメータ |

## データ準備のベストプラクティス

### 推奨データ量
- **最小**: 予測期間の4倍以上のデータポイント
- **推奨**: 予測期間の10倍以上のデータポイント
- **例**: 24時間予測には最低96時間分（4日分）、推奨240時間分（10日分）

### データ品質
- タイムスタンプは時間順（昇順）に並べてください
- NaN値や無限値を含めないでください
- 一定間隔のサンプリングが推奨されます
- 極端な外れ値は事前に処理してください

### タイムスタンプ形式
ISO 8601形式を使用してください：
```
2023-01-01T00:00:00    # UTC
2023-01-01T00:00:00Z   # UTC明示
2023-01-01T09:00:00+09:00  # JST
```

## エラーハンドリング

### 一般的なエラー

| HTTPステータス | エラー内容 | 対処法 |
|---------------|-----------|--------|
| 400 | リクエストパラメータエラー | パラメータの形式や値を確認 |
| 404 | タスクが見つからない | 正しいtask_idを使用 |
| 500 | サーバー内部エラー | ログを確認、サポートに連絡 |

### 予測処理のエラー

予測処理中にエラーが発生した場合、タスクのステータスが`failed`になり、`error`フィールドにエラー詳細が記録されます：

```json
{
  "task_id": "12345678-1234-5678-9012-123456789012",
  "status": "failed",
  "error": "データポイントが不十分です（5点）。AutoGluonを使用した時系列予測には最低5つのデータポイントが必要です。",
  "message": "予測処理に失敗しました"
}
```

## パフォーマンスガイド

### 処理時間の目安

| モデル | 小規模データ（<100点） | 中規模データ（100-1000点） | 大規模データ（>1000点） |
|-------|-------------------|------------------------|----------------------|
| seasonal_naive | <30秒 | <1分 | <2分 |
| autoets | <1分 | <3分 | <5分 |
| npts | <2分 | <5分 | <10分 |
| recursive_tabular | <3分 | <6分 | <10分 |
| temporal_fusion_transformer | <5分 | <15分 | <20分 |

### 並列処理
APIサーバーは複数の予測タスクを並列で処理できます（最大4並列）。

---

**最終更新**: 2025年7月12日
**バージョン**: v1.0.0
**関連ファイル**: `src/api/routes.py`, `src/models/predictor.py`
